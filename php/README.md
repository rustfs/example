# RustFS PHP Client Examples

This directory contains examples demonstrating how to interact with a [RustFS](https://github.com/rustfs/rustfs) server using PHP.

## Prerequisites

- PHP 7.4+
- A running RustFS server (default: `http://localhost:9000`)

## Usage

Navigate to this directory and execute the PHP examples:

```bash
php example.php
```
