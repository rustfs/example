# RustFS Go Client Examples

This directory contains examples demonstrating how to interact with a [RustFS](https://github.com/rustfs/rustfs) server using Go.

## Prerequisites

- Go 1.19+
- A running RustFS server (default: `http://localhost:9000`)

## Usage

Navigate to this directory and execute the Go examples:

```bash
go run main.go
```
